function sum(input){
    let firsNum = input[0];
    let lastNumber = input[input.length-1];
    console.log(firsNum + lastNumber);
}

// const input = [20, 30, 40]
// not working with var for whatever reason
sum(20, 30, 40);